from setuptools import setup

# pyproject.toml is the single source of truth for all packaging metadata
# (name, version, dependencies, entry points, scripts). This shim exists only
# for backward compatibility with tooling that still expects setup.py to exist.
# Do NOT add metadata here — edit pyproject.toml instead.
setup()
